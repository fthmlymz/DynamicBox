import { WorkflowListModel } from './workflow-list-model';

describe('WorkflowListModel', () => {
  it('should create an instance', () => {
    expect(new WorkflowListModel()).toBeTruthy();
  });
});
