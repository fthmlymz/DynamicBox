import { MaterialDemandsModel } from './material-demands-model';

describe('MaterialDemandsModel', () => {
  it('should create an instance', () => {
    expect(new MaterialDemandsModel()).toBeTruthy();
  });
});
