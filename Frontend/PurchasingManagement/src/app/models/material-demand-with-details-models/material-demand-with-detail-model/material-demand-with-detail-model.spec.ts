import { MaterialDemandWithDetailModel } from './material-demand-with-detail-model';

describe('MaterialDemandWithDetailModel', () => {
  it('should create an instance', () => {
    expect(new MaterialDemandWithDetailModel()).toBeTruthy();
  });
});
